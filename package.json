{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:package": "turbo run dev --filter=@turn25/react-headless-carousel",
    "dev:website": "turbo run dev --filter=website...",
    "start:website": "turbo run start --filter=website",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "latest",
    "prettier": "latest",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "react-headless-carousel-slider"
}
